import Link from 'next/link';

const Logo = () => {
    return (
        <Link href="/" className="font-playfair text-3xl font-light tracking-wide">
            hues
        </Link>
    );
};

export default Logo;
